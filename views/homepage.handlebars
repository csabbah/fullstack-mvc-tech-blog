<div id='homepage-posts'>
  {{#each posts as |post|}}
    <div id='single-post'>
      <div id='post-header'>
        <h1 id='post-title'>{{post.title}}</h1>
        <p id='post-date'>{{format_date post.created_at}}</p>
      </div>
    </div>
  {{/each}}
</div>